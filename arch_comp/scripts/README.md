# ARCH-COMP node scripts (install_tool.sh, run_benchmark.sh, run_single_instance.sh,
# docker/bootstrap_node.sh). The tool defines a Docker base image; install clones
# the tool into it and runs install + license activation.
