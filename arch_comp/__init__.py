"""ARCH-COMP as a plugin for comp-eval-platform.

Like vnn-comp, the whole variant is one Competition subclass + step handlers +
node scripts — plus a per-category registry (categories.py), the seam that lets
ARCH's heterogeneous categories share one engine. No core changes.
"""
